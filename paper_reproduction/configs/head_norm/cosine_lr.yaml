experiment:
  name: paper_head_norm_cosine_lr
output:
  root: paper_reproduction/raw_metrics
data:
  name: cifar100
  root: data
  num_classes: 100
  image_size: 32
  batch_size: 128
  num_workers: 2
  augment: false
  normalize: true
  return_clean_labels: true
  val_fraction: 0.1
  val_seed: 0
  deterministic_validation: true
label_noise:
  rate: 0.0
  seed: 0
model:
  name: resnet18_cifar
  in_channels: 3
  num_classes: 100
  width: 64
  use_bn: true
  head_type: cosine
  head_logit_scale: 1.0
fls:
  mode: location
  boundaries:
    after_early:
      output_multiplier: 1.0
      init_scale: 1.0
    after_middle:
      output_multiplier: 1.0
      init_scale: 1.0
    after_late:
      output_multiplier: 1.0
      init_scale: 1.0
training:
  seed: 20
  device: auto
  max_epochs: 80
  max_steps: null
  optimizer: sgd
  lr: 0.04096
  location_lr_compensation: upstream
  weight_decay: 0.0
  momentum: 0.0
  save_checkpoints: true
  evaluate_test_each_epoch: false
  protocol_sanity:
    enabled: true
  location_lr_compensation_multipliers:
    after_early: 1.0
    after_middle: 1.0
    after_late: 0.0625
diagnostics:
  enabled: false
