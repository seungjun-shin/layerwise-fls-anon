experiment:
  name: paper_reproduction_cifar100_fullgrid
output:
  root: outputs
data:
  name: cifar100
  root: data
  num_classes: 100
  image_size: 32
  batch_size: 128
  num_workers: 2
  augment: false
  normalize: true
  return_clean_labels: true
label_noise:
  rate: 0.0
  seed: 0
model:
  name: resnet18_cifar
  in_channels: 3
  num_classes: 100
  width: 64
  use_bn: true
fls:
  mode: global
  global:
    output_multiplier: 1.0
    lr_compensation: true
training:
  seed: 0
  device: auto
  max_epochs: 80
  max_steps: null
  optimizer: sgd
  lr: 0.00064
  weight_decay: 0.0
  momentum: 0.0
  target_train_accuracy: null
  target_train_loss: null
diagnostics:
  enabled: false
  frequency_epochs: 10
  groups: [early, middle, late, head]
sweep:
  seeds: [0, 1, 2]
  # Original-paper-style grid: c in powers of two and eta = k * 2^m,
  # where k = 6.4e-4. The optimizer uses eta / c.
  global_output_multipliers: [0.0009765625, 0.00390625, 0.015625, 0.0625, 0.25, 1.0, 4.0]
  learning_rates: [0.00064, 0.00256, 0.01024, 0.04096, 0.16384, 0.65536, 2.62144]
  record_threshold_train_accuracy: 0.99
